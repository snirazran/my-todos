# Project guidelines

## Code style

- Do not add explanatory comments to code. Omit narration comments (the `// do X so that Y` kind) and multi-line block comments that explain a change, design decision, or trade-off. Put that reasoning in the chat reply, not the code. Only add a comment when explicitly asked, and otherwise match the surrounding file's existing style.
